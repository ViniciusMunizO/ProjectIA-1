PORT=4000
NODE_ENV=development
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-your-service-role-key
COOKIE_SECRET=replace-with-a-long-random-value
SESSION_TTL_HOURS=24
SESSION_IDLE_TIMEOUT_HOURS=2
# Comma-separated if you need more than one (e.g. local dev + the deployed
# GitHub Pages URL): CORS_ORIGIN=http://localhost:5173,https://user.github.io
CORS_ORIGIN=http://localhost:5173
